using UnityEngine;

// Turret should already have a grid before we spawn it as we see it when we pick a turret. 
public class TurretGridManager : MonoBehaviour
{
    public TurretGridData MyGridData = new TurretGridData();

}
